<% content_for :javascript_includes do %>
  <%= javascript_include_tag "owl.carousel", "slick" %>
  <%= stylesheet_link_tag  "owl.carousel", "animate", "owl.theme", "slick", "slick-theme" %>
<% end %>

<script>
  jQuery(document).ready(function($) {
    $('.banner, .services-slider').owlCarousel({
      animateOut: 'fadeOut',
      animateIn: 'fadeIn',
      loop: true,
      autoplay: false,
      autoplayHoverPause: true,
      items: 1,
      nav : true,
      dotsEach: true,
      autoplaySpeed: 50000,
      navText : ['<span class="fa-stack"><i class="fa fa-circle fa-stack-1x"></i><i class="fa fa-chevron-circle-left fa-stack-1x fa-inverse"></i></span>','<span class="fa-stack"><i class="fa fa-circle fa-stack-1x"></i><i class="fa fa-chevron-circle-right fa-stack-1x fa-inverse"></i></span>']
    });
  });
</script>

<!-- header -->
<section id="home">
  <div class="banner owl-carousel owl-theme">
    <% @banners.each do |banner| %>
      <div class="item">
        <%# <div class="custom-overlay-inner">
          &nbsp;
        </div> %>
        <%= image_tag banner.image.url(:large) %>
        <div class="img-gradation"></div>
      </div>
    <% end %>
  </div>
</section>

<section id="services" class="main-section bg-black-gradient">
  <div class="col-12">
    <div class="row justify-content-center">
      <h1>OUR SERVICES</h1>
      <div class="services-slider owl-carousel owl-theme">
        <% @services.each do |service| %>
          <div class="item">
            <%# <div class="custom-overlay-inner">
              &nbsp;
            </div> %>
            <%= image_tag service.image.url(:large) %>
          <div class="img-gradation-white"></div>
          </div>
        <% end %>
      </div>
		</div>
  </div>
</section>

<section id="product" class="main-section">
  <div class="col-12">
    <div class="row justify-content-center img-slider">
      <h1>OUR PRODUCTS</h1>
      <div class="col-10 center">
        <% @products.each do |product| %>
          <div><%= image_tag product.get_image("medium"), class: "img-fluid" %></div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="col-12 faq-home">
    <h2 class="header">FAQs</h2>
    <div class="text">
      is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the
      industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of
      type and scrambled it to make a type specimen book<br /><br />
      <%= link_to "FIND OUT MORE", faq_path, class: "btn btn-outline-black" %>
    </div>
  </div>
</section>

<section id="hubungi-kami" class="main-section">
  <div class="container">
    <%= render :partial => "shared/flash" %>
    <div class="row">
      <div class="col-12">
        <h2 class="header">GET IN TOUCH</h2>
      </div>
      <div class="col-12 col-lg-7">
        <div class="ca-form">
          <div class="cf-msg" style="display: none;"></div>
          <%= simple_form_for @contact, url: save_contact_us_path(anchor: "hubungi-kami"), wrapper: :vertical_form,
                    wrapper_mappings: { check_boxes: :vertical_collection } do |f| %>
            <div class="row">
              <div class="col-12"><%= f.error_notification %></div>
              <div class="col-6">
                <div class="ca-input-box">
                  <%= f.input :name, label_html: { class: 'sr-only' }, placeholder: "Name" %>
                </div>
              </div>
              <div class="col-6">
                <div class="ca-input-box">
                  <%= f.input :phone, label_html: { class: 'sr-only' }, placeholder: "Phone Number" %>
                </div>
              </div>
              <div class="col-12">
                <div class="ca-input-box">
                  <%= f.input :email, label_html: { class: 'sr-only' }, placeholder: "Email" %>
                </div>
              </div>
              <div class="col-12">
                <div class="ca-input-box">
                  <%= f.input :content, label_html: { class: 'sr-only' }, placeholder: "Write Message", 
                              as: "text", :input_html => { :rows => 9 } %>
                </div>
              </div>

              <div class="col-md-12 mb-3">
                <span><%#= recaptcha_tags %></span>
              </div>

              <div class="col-md-12">
                <button type="submit" class="btn btn-outline-yellow">Send Message</button>
              </div>
            </div>
          <% end %>
        </div>
      </div>
      <div class="col-12 col-lg-5">
        <div class="row">
          <div class="col-12">
            <div class="ca-info">
              <ul class="info">
                <li>
                  <span class="icon"><%= fa_icon "map-marker" %></span>
                  <span class="title">OUR LOCATION</span>
                  <span class="details">
                    Jl.Jend Sudirman No.107, Karanganyar<br /> 
                    Kota Bandung, Jawa Barat 40212
                    Indonesia
                  </span>
                </li>
                <li>
                  <span class="icon"><%= fa_icon "phone" %></span>
                  <span class="title">CALL US ON</span>
                  <span class="details">+62 081 234567<br />+62 081 234678</span>
                </li>
                <li>
                  <span class="icon"><%= fa_icon "envelope-o" %></span>
                  <span class="title">SEND YOUR MESSAGE</span>
                  <span class="details">info@luminous-id.com<br />admin@luminous-id.com</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  $(document).ready(function () {
    $('.center').slick({
      centerMode: true,
      slidesToShow: 3,
      dots: true,
      responsive: [
        {
          breakpoint: 768,
          settings: {
            arrows: false,
            centerMode: true,
            centerPadding: '40px',
            slidesToShow: 3
          }
        },
        {
          breakpoint: 480,
          settings: {
            arrows: false,
            centerMode: true,
            centerPadding: '40px',
            slidesToShow: 1
          }
        }
      ]
    });


    if(window.location.href.indexOf("#") >= 0){
      anr = "#" + window.location.href.split("#")[1];
      $('html, body').animate({
          scrollTop: $(anr).offset().top - 80
      }, 600);
    }
    $(window).on('scroll', function () {
      mainSection = $('.main-section'),
      mainSectionLength = mainSection.length,
      links = $('#menu li'),
      flag = 0,
      flag2 = 0,
      lastI = 100,
      myWindow = $(window);

// change navbar active link while scrolling
      if (flag === 0 && mainSectionLength) {
          flag = 1;
          flag2 = 0;
          for (let i = mainSectionLength - 1; i >= 0; i--) {
              if (myWindow.scrollTop() >= mainSection.eq(i).offset().top-81) {
                  if (i != lastI) {
                      links.eq(i + 1).addClass('active').siblings().removeClass('active');
                      lastI = i;
                  }
                  flag2 = 1;
                  break;
              }
          }
          if (flag2 === 0) {
              links.eq(0).addClass('active').siblings().removeClass('active');
              lastI = -1;
          }
          flag = 0;
      }
    });
  });
</script>
